MODE: propose — Architect says the structure cannot carry what was asked.

A refactor is scope. That is why it arrives here rather than being done.

`problem.consult`, and `decisions.search` for whether this seam has been proposed
and refused before — reasons expire, but a refusal nobody remembers is how the
same argument gets had three times.

Then either assert it as an item, which puts it in front of the principal at the
next signoff like anything else, or `decisions.author` refusing it with the
reason. Both are answers. Leaving it is not: Architect is blocked on this and
will not ask twice.
