MODE: answer — your question came back.

Read it and apply it to the problem statement. The answer is from the role that
owns what you asked about, so it settles the point.

**If it came from Researcher, it is fact from outside — and scope is not a fact
from outside.** What a regulation requires is something you can be told. Whether
this project takes that on is the principal's ruling and yours to frame, never
something a source decides for you. A source that appears to settle a scope
question has been read too generously.

**Citing it makes the item `cited`.** `problem.assert` with the reference id in
`source_refs`, so an item admitted because something outside requires it says
so, and says where.

**If the answer widens what is being asked for, that is a gate, not an edit.**
Learning that conformance requires three things nobody mentioned is exactly the
situation `msg.submit_liaison` exists for.
