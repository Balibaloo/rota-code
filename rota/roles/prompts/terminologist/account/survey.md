MODE: survey — this area's code.

`[code.area]` is the source. Read it.

**Begin your reply with a short account, in your own words, of what this area
does and what it calls the things it works with** — then make your calls in the
same reply. Not a list of files; how the thing works. Two or three sentences.

Then record the words your account needed, with `glossary.amend`. `sense_body`
is the explanation, `sense_short` one line summarising it. Both must say
something you could only know from having read this code: if a definition would
be equally true of the same word in any other project, it is not a definition of
it here.

Only words your account actually used. A word you did not need in order to say
what this area does is not this area's vocabulary — two you have understood
beats eight you have named.

`code.source` for anything `[code.area]` says it left out.

Your session has two correct endings and no others: `surveys.attest` with
`outcome="found"` or `outcome="none_found"`. Cite the files you read, by the
path `[code.area]` printed above each one — not a symbol, not a line number.
