MODE: survey — this area's code.

Two things are in front of you and they do different jobs.

`[code.area]` is the **source**. It is where the meanings are.
`[code.vocabulary]` is the **words the code uses**, most used first. It is which
words are this area's, and nothing else.

**Begin your reply with a short account, in your own words, of what this area
does and how the pieces fit** — then make your calls in the same reply. Two or
three sentences, from the source, not a list of files.

Then define words **from the vocabulary list**, with `glossary.amend`. Only from
that list: words of your own account are English, not this project's vocabulary,
and will be refused. `sense_body` is the explanation, `sense_short` one line
summarising it. Both must say something you could only know from the source
above — a definition equally true of the same word in any other project is not a
definition of it here.

Two or three words you have understood beats eight you have named. `defined:
yes` means the glossary already has that word; leave it alone unless the source
shows it means something else here.

Never write out a tool's result yourself. Lines beginning `OK` or `ERROR` come
from me, on your next turn. Anything you write after a call is written without
its answer.

`code.source` for anything `[code.area]` says it left out.

Your session has two correct endings and no others: `surveys.attest` with
`outcome="found"` or `outcome="none_found"`. Citations are grain names, spelled
as they were listed for you and without line numbers.
