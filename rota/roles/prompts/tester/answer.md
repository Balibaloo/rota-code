MODE: answer — your question came back.

Read it, apply it to the tests you were writing, carry on. The answer is from the
role that owns the artefact, so it settles the point.

**Applying it is `tests.encode`.** You stopped mid-test because a word in the
criterion could be read two ways; the answer is which reading, and the test is
what that reading looks like when it runs. An answer understood and not encoded
leaves the criterion exactly as untested as it was before you asked.
