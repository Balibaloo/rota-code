[
 {
  "id": "define:text",
  "kind": "define",
  "system": "You are terminologist.\n\nYou are Terminologist. You own the glossary: what each word this project uses\nmeans, here.\n\nYou are woken once, act, and end. You have no memory of previous sessions and\nwill have none of this one.\n\nMODE: define \u2014 one word, for the whole program.\n\n`[problem.baseline]` is what this program does for its user, as the Vision Keeper\nobserved it. `[code.concordance]` is the word you were woken for, everywhere\nthis project uses it, with what settles it first: what the code declares it to\nbe a kind of, what a user writes that says it, a file named for it, where it is\nregistered, then its declarations and its uses. The word is named on the\n`The word:` line. Weigh the top of the concordance over the bottom \u2014 a word the\ncode lists among kinds, or a user writes as an option, means that kind here\nwhatever the same word means in English.\n\nA word this project means nothing of its own by \u2014 ordinary English here, or a\nlibrary's word rather than this program's \u2014 is `surveys.attest(\noutcome=\"none_found\")`, and that is rare: the word was woken because the\nproject declares it. Otherwise, finish this sentence, in your own words, from\nthe concordance and the baseline:\n\n    In this project, a <word> is ...\n\nDefine the thing the code declares, not the program as a whole and not what the\nuser wants from it. Say what kind of thing it is, where a user or the code\nwrites it down \u2014 the exact key, type or file, spelled as the concordance spells\nit \u2014 and what it is for.\nThirty words at most. Test it by putting a different word in its place: if the\nsentence still reads true, it is a category and not a meaning.\n\nA concordance headed \"the code never says this word; the prose does\" is the\nREADME's word, not the code's. The question is whether the README uses it as a\nname for something this project declares under its own word. If it does, the\nsense says exactly that -- `In this project, a <word> is the README's name for\n<the code's word> ...` -- and still names the place the code's word is written\ndown: pull `code.concordance` for the code's own word if you need its key or\nfile. If the prose word names nothing the code shows, `surveys.attest(\noutcome=\"none_found\")` -- for a prose word that is the common honest ending,\nnot the rare one.\n\n`code.source` a file the concordance makes you want to read whole \u2014 the one\nthat declares the word is usually enough.\n\n`[code.concordance]` also carries the file that declares the word, whole where\nit fits. Read it: what the word *does* is in that file, not in the three lines\nthat mention it.\n\nEnd with one `glossary.amend`, for this word only: `sense_short` the sentence\nin one clause; `sense_body` two to four sentences a developer who has never\nseen the code could act on -- where it is written down and by whom, what the\ncode does with it, what it relates to (the other words of this project it is\nmade of or makes), and what would break if it meant the everyday thing here.\nThat is the session's result, and the session ends with it.",
  "user": "You were woken by: tick:define\nRefs: @term:text\nThe word: text\n\n[code.concordance]\ndeclared as a kind:\n  src/variables/providers/index.ts:28  export enum TemplateVariableType {\n      29: text = \"text\",\nin what a user writes:\n  intentsSchema.yaml:5  intents_imported_from: \"'text'|['text']\"\n  intentsSchema.yaml:7  - make_a: \"!text\"\n  intentsSchema.yaml:10  with_name: \"text ({{intent_name}})\"\n  intentsSchema.yaml:11  in_folder: \"text (./)\"\n  intentsSchema.yaml:12  replaces_selection_with: \"text ([[{{with_name}}]])\"\n  intentsSchema.yaml:14  - called: \"!text\"\n  intentsSchema.yaml:17  at_path: \"!text\"\n  intentsSchema.yaml:18  with_name: \"text ({{intent_name}})\"\n  intentsSchema.yaml:19  in_folder: \"text (./)\"\n  intentsSchema.yaml:20  replaces_selection_with: \"text ([[{{with_name}}]])\"\na file of its own:\n  src/variables/providers/text.ts\n      5: export type TemplateVariableVariables_Text = {\n      9: export const parseTextVariableFrontmatter = (app: App, fm:any) => ({\n      32: function validateText(app: App, variable: TemplateVariable & TemplateVariableVariables_Text , value: string, t\nregistered:\n  src/variables/providers/index.ts:45  [TemplateVariableType.text]: TemplateVariableVariables_Text,\n  src/variables/providers/index.ts:57  [TemplateVariableType.text]: parseTextVariableFrontmatter,\n  src/variables/providers/index.ts:67  [TemplateVariableType.text]: getTextVariableValue,\n  src/variables/suggest.ts:119  text: string,\ndeclared:\n  src/intents/intents.ts:128  const selectionText = plugin.app.workspace.activeEditor?.editor?.getRange( selectionStart, selectionEnd ) || \"\n  src/intents/intents.ts:178  const replacementText = gatheredValues[ReservedVariableName.replaces_selection_with];\n  src/intents/intents.ts:194  export function getReplacedVariablesText(text: string, values:{[key: string]: string}): string{\n  src/variables/suggest.ts:91  const textComponent = new TextComponent(container);\n  src/intents/intents.ts:129  const selectionSplit = selectionText.split(new RegExp(`[${plugin.settings.selectionDelimiters}]`,\"g\"))\n  src/intents/intents.ts:166  const newNoteContents = getReplacedVariablesText(templateContents, gatheredValues);\nused:\n  intentsSchema.yaml  [.]  x31\n  src/variables/suggest.ts  [src/variables]  x18\n      91: const textComponent = new TextComponent(container);\n      16: private validator: ((text:string) => boolean) | undefined;\n  src/intents/intents.ts  [src/intents]  x10\n      194: export function getReplacedVariablesText(text: string, values:{[key: string]: string}): string{\n      128: const selectionText = plugin.app.workspace.activeEditor?.editor?.getRange( selectionStart, selectionEnd ) || \"\n  src/variables/providers/index.ts  [src/variables/providers]  x9\n  src/variables/providers/text.ts  [src/variables/providers]  x9\n  src/settings/settings.ts  [src]  x6\n      139: .setDesc(fragWithHTML(\"You can create multiple notes by selecting text with <a href='https://help.obsidian.md/\n      36: text.setPlaceholder('Path')\nflows:\n  src/intents/frontmatter.ts -> intentsSchema.yaml\n  src/intents/frontmatter.ts -> src/intents/index.ts\n  src/intents/index.ts -> src/intents/frontmatter.ts\n  src/intents/index.ts -> src/intents/intents.ts\n  src/intents/intents.ts -> src/intents/index.ts\n  src/templates/templates.ts -> src/intents/index.ts\n  src/variables/providers/index.ts -> src/variables/providers/natural_date.ts\n  src/variables/providers/index.ts -> src/variables/providers/number.ts\n\n----- src/variables/providers/text.ts -----\nimport { App, Notice } from \"obsidian\";\nimport { TemplateVariable } from \"..\";\nimport { GenericInputPrompt } from \"../suggest\";\n\nexport type TemplateVariableVariables_Text = {\n  regex ?: string,\n};\n\nexport const parseTextVariableFrontmatter = (app: App, fm:any) => ({\n  regex: fm.matches_regex,\n})\n\nexport async function getTextVariableValue(app: App, variable: TemplateVariable&TemplateVariableVariables_Text, existingValue:string, sourceNotePath: string):Promise<string>{\n  if (!validateText(app, variable, existingValue, false)) {\n    if ( existingValue )\n      new Notice(`Invalid selection: ${existingValue}`);\n\n    try {\n      existingValue = await GenericInputPrompt.Prompt(app, variable,\n        text => validateText(app, variable, text, false),\n        \"Error: Please enter text\" + (variable.regex ? ` that matches the following regex \"${variable.regex}\"` : \"\")\n      );\n    } catch (e){\n      console.log(e);\n    }\n    validateText(app, variable, existingValue, true);\n  };\n\n  return existingValue\n}\n\nfunction validateText(app: App, variable: TemplateVariable & TemplateVariableVariables_Text , value: string, throwErrors: boolean): boolean {\n  if (variable.regex && !Boolean(value.match(variable.regex))) {\n    if (variable.required && throwErrors)\n      throw new Error(`Error: value for ${variable.name} doesn't match the regular expression \"${variable.regex}\"`)\n    return false;\n\n  } else if (value === \"\" || !value) {\n    if (variable.required && throwErrors)\n      throw new Error(`Error: missing required text variable ${variable.name}`);\n    return false;\n  }\n\n\n  return true;\n}\n",
  "truth": {
   "must_not_contain": [
    "string",
    "characters",
    "data type",
    "primitive",
    "sequence of"
   ],
   "is_about": "a prompt type whose value is typed in, optionally matched against a regex"
  }
 },
 {
  "id": "define:note",
  "kind": "define",
  "system": "You are terminologist.\n\nYou are Terminologist. You own the glossary: what each word this project uses\nmeans, here.\n\nYou are woken once, act, and end. You have no memory of previous sessions and\nwill have none of this one.\n\nMODE: define \u2014 one word, for the whole program.\n\n`[problem.baseline]` is what this program does for its user, as the Vision Keeper\nobserved it. `[code.concordance]` is the word you were woken for, everywhere\nthis project uses it, with what settles it first: what the code declares it to\nbe a kind of, what a user writes that says it, a file named for it, where it is\nregistered, then its declarations and its uses. The word is named on the\n`The word:` line. Weigh the top of the concordance over the bottom \u2014 a word the\ncode lists among kinds, or a user writes as an option, means that kind here\nwhatever the same word means in English.\n\nA word this project means nothing of its own by \u2014 ordinary English here, or a\nlibrary's word rather than this program's \u2014 is `surveys.attest(\noutcome=\"none_found\")`, and that is rare: the word was woken because the\nproject declares it. Otherwise, finish this sentence, in your own words, from\nthe concordance and the baseline:\n\n    In this project, a <word> is ...\n\nDefine the thing the code declares, not the program as a whole and not what the\nuser wants from it. Say what kind of thing it is, where a user or the code\nwrites it down \u2014 the exact key, type or file, spelled as the concordance spells\nit \u2014 and what it is for.\nThirty words at most. Test it by putting a different word in its place: if the\nsentence still reads true, it is a category and not a meaning.\n\nA concordance headed \"the code never says this word; the prose does\" is the\nREADME's word, not the code's. The question is whether the README uses it as a\nname for something this project declares under its own word. If it does, the\nsense says exactly that -- `In this project, a <word> is the README's name for\n<the code's word> ...` -- and still names the place the code's word is written\ndown: pull `code.concordance` for the code's own word if you need its key or\nfile. If the prose word names nothing the code shows, `surveys.attest(\noutcome=\"none_found\")` -- for a prose word that is the common honest ending,\nnot the rare one.\n\n`code.source` a file the concordance makes you want to read whole \u2014 the one\nthat declares the word is usually enough.\n\n`[code.concordance]` also carries the file that declares the word, whole where\nit fits. Read it: what the word *does* is in that file, not in the three lines\nthat mention it.\n\nEnd with one `glossary.amend`, for this word only: `sense_short` the sentence\nin one clause; `sense_body` two to four sentences a developer who has never\nseen the code could act on -- where it is written down and by whom, what the\ncode does with it, what it relates to (the other words of this project it is\nmade of or makes), and what would break if it meant the everyday thing here.\nThat is the session's result, and the session ends with it.",
  "user": "You were woken by: tick:define\nRefs: @term:note\nThe word: note\n\n[code.concordance]\ndeclared as a kind:\n  src/variables/providers/index.ts:28  export enum TemplateVariableType {\n      32: note = \"note\",\nin what a user writes:\n  intentsSchema.yaml:25  of_type: \"text|number|natural_date|note|folder (text)\"\n  intentsSchema.yaml:48  # Note and Folder\n  intentsSchema.yaml:53  ## Note only\n  intentsSchema.yaml:54  include_note_name: \"text\"\n  intentsSchema.yaml:55  exclude_note_name: \"text\"\n  intentsSchema.yaml:58  note_output_format: \"path|name (path)\"\na file of its own:\n  src/variables/providers/note.ts\n      5: export type TemplateVariableVariables_Note = {\n      16: export const parseNoteVariableFrontmatter = (app: App, fm:any) : TemplateVariableVariables_Note => ({\n      28: const parentFolderPath = sourceNotePath?.split(\"/\").slice(0, -1).join(\"/\") ?? \"/\";\n      35: const filteredOpener = (app as any).plugins.plugins[\"filtered-opener\"];\n      41: const selectedNote = await filteredOpener.api_getNote({\nregistered:\n  src/variables/providers/index.ts:48  [TemplateVariableType.note]: TemplateVariableVariables_Note,\n  src/variables/providers/index.ts:60  [TemplateVariableType.note]: parseNoteVariableFrontmatter,\n  src/variables/providers/index.ts:70  [TemplateVariableType.note]: getNoteVariableValue,\ndeclared:\n  src/intents/intents.ts:167  const newNoteFileName = gatheredValues[ReservedVariableName.with_name];\n  src/intents/index.ts:15  export type hasNewNoteProperties = {\n  src/intents/index.ts:19  export type NewNoteProperties = {\n  src/intents/intents.ts:7  type NoteDestination = {\n  src/intents/frontmatter.ts:56  function getNewNotePropertiesFromFM(app: App, fm: FrontMatterCache): NewNoteProperties {\n  src/intents/frontmatter.ts:144  const newNoteFolderPath = getRelativePath( path, parentFolder?.path)\nused:\n  src/main.ts  [.]  x34\n      29: const intentNote = this.app.workspace.getActiveFile()\n      36: const noteIntents = await getIntentsFromTFile(this.app, intentNote);\n  src/intents/intents.ts  [src/intents]  x32\n      60: const abstractIntentSource = plugin.app.vault.getAbstractFileByPath( intent.sourceNotePath );\n      83: const templateNote = this.app.vault.getAbstractFileByPath(templatePath+\".md\");\n  src/settings/settings.ts  [src]  x30\n      56: const options : [string, string][] = ( filteredOpener?.api_getListOfNoteFilterSets() || [] )\n      40: this.plugin.settings.globalIntentsNotePath = value + (!value.endsWith(\".md\") ? \".md\" : \"\");\n  src/variables/providers/note.ts  [src/variables/providers]  x26\n      80: function validateNote(app: App, variable: TemplateVariable & TemplateVariableVariables_Note, value: string, re\n  src/intents/frontmatter.ts  [src/intents]  x14\n      56: function getNewNotePropertiesFromFM(app: App, fm: FrontMatterCache): NewNoteProperties {\n      102: const fm = app.metadataCache.getFileCache(note)?.frontmatter || {};\n  src/variables/providers/index.ts  [src/variables/providers]  x9\n      65: [K in keyof TemplateVariableVariablesLut]: (app: App, variable: TemplateVariable & TemplateVariableVariablesLu\nflows:\n  src/intents/frontmatter.ts -> intentsSchema.yaml\n  src/intents/frontmatter.ts -> src/intents/index.ts\n  src/intents/frontmatter.ts -> src/templates/index.ts\n  src/intents/index.ts -> src/intents/frontmatter.ts\n  src/intents/index.ts -> src/intents/intents.ts\n  src/intents/index.ts -> src/templates/index.ts\n  src/intents/intents.ts -> src/intents/index.ts\n  src/intents/intents.ts -> src/main.ts\n\n----- src/variables/providers/note.ts (first part only) -----\nimport { App, Notice, TFile } from \"obsidian\";\nimport { getRelativePath, TemplateVariable } from \"..\";\nimport { FilteredOpenerMissingNotice } from \"src/notice\";\n\nexport type TemplateVariableVariables_Note = {\n  filter_set_name: string,\n  include_path_name: string,\n  exclude_path_name: string,\n  include_note_name: string,\n  exclude_note_name: string,\n  include_tags: string,\n  exclude_tags: string,\n  note_output_format: string,\n};\n\nexport const parseNoteVariableFrontmatter = (app: App, fm:any) : TemplateVariableVariables_Note => ({\n  filter_set_name: fm.filter_set_name,\n  include_path_name: fm.include_path_name,\n  exclude_path_name: fm.exclude_path_name,\n  include_note_name: fm.include_note_name,\n  exclude_note_name: fm.exclude_note_name,\n  include_tags: fm.include_tags,\n  exclude_tags: fm.exclude_tags,\n  note_output_format: fm.note_output_format,\n})\n\nexport async function getNoteVariableValue( app:App, variable:TemplateVariable&TemplateVariableVariables_Note, existingValue:string, sourceNotePath:string ): Promise<string> {\n  const parentFolderPath = sourceNotePath?.split(\"/\").slice(0, -1).join(\"/\") ?? \"/\";\n\n  if (!validateNote(app, variable, existingValue, parentFolderPath, false)) {\n    if ( existingValue )\n      new Notice(`Invalid selection: ${existingValue}`);\n    \n    try {\n      const filteredOpener = (app as any).plugins.plugins[\"filtered-opener\"];\n      if (!filteredOpener) {\n        new FilteredOpenerMissingNotice();\n      }\n       \n\n      const selectedNote = await filteredOpener.api_getNote({\n        name: variable.filter_set_name,\n        includePathName: getRelativePath( variable.include_path_name, parentFolderPath ),\n        excludePathName: getRelativePath( variable.exclude_path_name, parentFolderPath ),\n        includeNoteName: variable.include_note_name,\n        excludeNoteName: variable.exclude_note_name,\n        includeTags: variable.include_tags,\n        excludeTags: variable.exclude_tags,\n      });\n\n      if (!(selectedNote instanceof TFile))\n   ",
  "truth": {
   "must_not_contain": [
    "text note",
    "markdown file",
    "a note in the vault",
    "document",
    "data container"
   ],
   "is_about": "a prompt type whose value is a note chosen through the filtered-opener plugin, narrowed by a filter set"
  }
 },
 {
  "id": "define:folder",
  "kind": "define",
  "system": "You are terminologist.\n\nYou are Terminologist. You own the glossary: what each word this project uses\nmeans, here.\n\nYou are woken once, act, and end. You have no memory of previous sessions and\nwill have none of this one.\n\nMODE: define \u2014 one word, for the whole program.\n\n`[problem.baseline]` is what this program does for its user, as the Vision Keeper\nobserved it. `[code.concordance]` is the word you were woken for, everywhere\nthis project uses it, with what settles it first: what the code declares it to\nbe a kind of, what a user writes that says it, a file named for it, where it is\nregistered, then its declarations and its uses. The word is named on the\n`The word:` line. Weigh the top of the concordance over the bottom \u2014 a word the\ncode lists among kinds, or a user writes as an option, means that kind here\nwhatever the same word means in English.\n\nA word this project means nothing of its own by \u2014 ordinary English here, or a\nlibrary's word rather than this program's \u2014 is `surveys.attest(\noutcome=\"none_found\")`, and that is rare: the word was woken because the\nproject declares it. Otherwise, finish this sentence, in your own words, from\nthe concordance and the baseline:\n\n    In this project, a <word> is ...\n\nDefine the thing the code declares, not the program as a whole and not what the\nuser wants from it. Say what kind of thing it is, where a user or the code\nwrites it down \u2014 the exact key, type or file, spelled as the concordance spells\nit \u2014 and what it is for.\nThirty words at most. Test it by putting a different word in its place: if the\nsentence still reads true, it is a category and not a meaning.\n\nA concordance headed \"the code never says this word; the prose does\" is the\nREADME's word, not the code's. The question is whether the README uses it as a\nname for something this project declares under its own word. If it does, the\nsense says exactly that -- `In this project, a <word> is the README's name for\n<the code's word> ...` -- and still names the place the code's word is written\ndown: pull `code.concordance` for the code's own word if you need its key or\nfile. If the prose word names nothing the code shows, `surveys.attest(\noutcome=\"none_found\")` -- for a prose word that is the common honest ending,\nnot the rare one.\n\n`code.source` a file the concordance makes you want to read whole \u2014 the one\nthat declares the word is usually enough.\n\n`[code.concordance]` also carries the file that declares the word, whole where\nit fits. Read it: what the word *does* is in that file, not in the three lines\nthat mention it.\n\nEnd with one `glossary.amend`, for this word only: `sense_short` the sentence\nin one clause; `sense_body` two to four sentences a developer who has never\nseen the code could act on -- where it is written down and by whom, what the\ncode does with it, what it relates to (the other words of this project it is\nmade of or makes), and what would break if it meant the everyday thing here.\nThat is the session's result, and the session ends with it.",
  "user": "You were woken by: tick:define\nRefs: @term:folder\nThe word: folder\n\n[code.concordance]\ndeclared as a kind:\n  src/variables/index.ts:7  export enum ReservedVariableName {\n      10: in_folder = \"in_folder\",\n  src/variables/providers/index.ts:28  export enum TemplateVariableType {\n      33: folder = \"folder\",\nin what a user writes:\n  intentsSchema.yaml:11  in_folder: \"text (./)\"\n  intentsSchema.yaml:19  in_folder: \"text (./)\"\n  intentsSchema.yaml:25  of_type: \"text|number|natural_date|note|folder (text)\"\n  intentsSchema.yaml:48  # Note and Folder\n  intentsSchema.yaml:60  ## Folder only\n  intentsSchema.yaml:61  include_folder_name: \"text\"\n  intentsSchema.yaml:62  exclude_folder_name: \"text\"\n  intentsSchema.yaml:63  in_folder: \"text (/)\"\n  intentsSchema.yaml:66  folder_output_format: \"path|name (path)\"\na file of its own:\n  src/variables/providers/folder.ts\n      5: export type TemplateVariableVariables_Folder = {\n      17: export const parseFolderVariableFrontmatter = (app: App, fm:any) => ({\n      32: const parentFolderPath = sourceNotePath?.split(\"/\").slice(0, -1).join(\"/\") ?? \"/\";\n      39: const filteredOpener = (app as any).plugins.plugins[\"filtered-opener\"];\n      44: const newProjectFolder = await filteredOpener.api_getFolder({\nregistered:\n  src/variables/providers/index.ts:49  [TemplateVariableType.folder]: TemplateVariableVariables_Folder,\n  src/variables/providers/index.ts:61  [TemplateVariableType.folder]: parseFolderVariableFrontmatter,\n  src/variables/providers/index.ts:71  [TemplateVariableType.folder]: getFolderVariableValue,\ndeclared:\n  src/intents/frontmatter.ts:143  const parentFolder = projectFile instanceof TFile ? projectFile.parent : projectFile;\n  src/intents/frontmatter.ts:144  const newNoteFolderPath = getRelativePath( path, parentFolder?.path)\n  src/intents/intents.ts:152  const newNoteFolderRelativePath = gatheredValues[ReservedVariableName.in_folder];\n  src/intents/intents.ts:153  const newNoteFolder = resolvePathRelativeToAbstractFile( newNoteFolderRelativePath, abstractIntentSource);\n  src/variables/providers/folder.ts:83  function validateFolder(app: App, variable: TemplateVariable & TemplateVariableVariables_Folder, value: string\n  src/variables/providers/note.ts:28  const parentFolderPath = sourceNotePath?.split(\"/\").slice(0, -1).join(\"/\") ?? \"/\";\nused:\n  src/variables/providers/folder.ts  [src/variables/providers]  x32\n      83: function validateFolder(app: App, variable: TemplateVariable & TemplateVariableVariables_Folder, value: string\n  src/intents/intents.ts  [src/intents]  x10\n      152: const newNoteFolderRelativePath = gatheredValues[ReservedVariableName.in_folder];\n      153: const newNoteFolder = resolvePathRelativeToAbstractFile( newNoteFolderRelativePath, abstractIntentSource);\n  intentsSchema.yaml  [.]  x9\n  src/variables/providers/index.ts  [src/variables/providers]  x9\n  src/variables/providers/note.ts  [src/variables/providers]  x6\n      28: const parentFolderPath = sourceNotePath?.split(\"/\").slice(0, -1).join(\"/\") ?? \"/\";\n      70: const absolutePath = getRelativePath( existingValue, parentFolderPath ) as string;\n  src/intents/frontmatter.ts  [src/intents]  x5\n      143: const parentFolder = projectFile instanceof TFile ? projectFile.parent : projectFile;\n      144: const newNoteFolderPath = getRelativePath( path, parentFolder?.path)\nflows:\n  src/intents/frontmatter.ts -> intentsSchema.yaml\n  src/intents/frontmatter.ts -> src/intents/index.ts\n  src/intents/frontmatter.ts -> src/variables/index.ts\n  src/intents/index.ts -> src/intents/frontmatter.ts\n  src/intents/index.ts -> src/intents/intents.ts\n  src/intents/index.ts -> src/variables/index.ts\n  src/intents/intents.ts -> src/intents/index.ts\n  src/intents/intents.ts -> src/variables/index.ts\n\n----- src/variables/providers/folder.ts (first part only) -----\nimport { App, Notice, TFolder } from \"obsidian\";\nimport { getRelativePath, TemplateVariable } from \"..\";\nimport { FilteredOpenerMissingNotice } from \"src/notice\";\n\nexport type TemplateVariableVariables_Folder = {\n  root_folder: string,\n  depth: number,\n  include_roots: boolean,\n  filter_set_name: string,\n  include_path_name: string,\n  exclude_path_name: string,\n  include_folder_name: string,\n  exclude_folder_name: string,\n  folder_output_format: string,\n};\n\nexport const parseFolderVariableFrontmatter = (app: App, fm:any) => ({\n  root_folder: fm.in_folder,\n  depth: fm.at_depth,\n  include_roots: typeof fm?.includes_roots === \"undefined\" ? undefined :\n    typeof fm?.includes_roots === \"boolean\" ? fm?.includes_roots :\n      Boolean(fm?.includes_roots?.[0]?.toUpperCase() === \"T\"),\n  filter_set_name: fm.filter_set_name,\n  include_path_name: fm.include_path_name,\n  exclude_path_name: fm.exclude_path_name,\n  include_folder_name: fm.include_folder_name,\n  exclude_folder_name: fm.exclude_folder_name,\n  folder_output_format: fm.folder_output_format,\n})\n\nexport async function getFolderVariableValue( app:App, variable:TemplateVariable&TemplateVariableVariables_Folder, existingValue:string, sourceNotePath:string ): Promise<string> {\n  const parentFolderPath = sourceNotePath?.split(\"/\").slice(0, -1).join(\"/\") ?? \"/\";\n\n  if (!validateFolder(app, variable, existingValue, parentFolderPath, false)) {\n    if ( existingValue )\n      new Notice(`Invalid selection: ${existingValue}`);\n    \n    try {\n      const filteredOpener = (app as any).plugins.plugins[\"filtered-opener\"];\n      if (!filteredOpener) {\n        new FilteredOpenerMissingNotice();\n      }\n\n      const newProjectFolder = await filteredOpener.api_getFolder({\n          name: variable.filter_set_name,\n          rootFolder: variable.root_folder,\n          includePathName: getRelativePath( variable.include_path_name, parentFolderPath ),\n          excludePathName: getRelativePath( variable.exclude_path_name, parentFolderPath ),\n ",
  "truth": {
   "must_not_contain": [
    "data container",
    "directory on disk",
    "container",
    "data type"
   ],
   "is_about": "a prompt type whose value is a folder chosen and validated against filters"
  }
 },
 {
  "id": "define:intent",
  "kind": "define",
  "system": "You are terminologist.\n\nYou are Terminologist. You own the glossary: what each word this project uses\nmeans, here.\n\nYou are woken once, act, and end. You have no memory of previous sessions and\nwill have none of this one.\n\nMODE: define \u2014 one word, for the whole program.\n\n`[problem.baseline]` is what this program does for its user, as the Vision Keeper\nobserved it. `[code.concordance]` is the word you were woken for, everywhere\nthis project uses it, with what settles it first: what the code declares it to\nbe a kind of, what a user writes that says it, a file named for it, where it is\nregistered, then its declarations and its uses. The word is named on the\n`The word:` line. Weigh the top of the concordance over the bottom \u2014 a word the\ncode lists among kinds, or a user writes as an option, means that kind here\nwhatever the same word means in English.\n\nA word this project means nothing of its own by \u2014 ordinary English here, or a\nlibrary's word rather than this program's \u2014 is `surveys.attest(\noutcome=\"none_found\")`, and that is rare: the word was woken because the\nproject declares it. Otherwise, finish this sentence, in your own words, from\nthe concordance and the baseline:\n\n    In this project, a <word> is ...\n\nDefine the thing the code declares, not the program as a whole and not what the\nuser wants from it. Say what kind of thing it is, where a user or the code\nwrites it down \u2014 the exact key, type or file, spelled as the concordance spells\nit \u2014 and what it is for.\nThirty words at most. Test it by putting a different word in its place: if the\nsentence still reads true, it is a category and not a meaning.\n\nA concordance headed \"the code never says this word; the prose does\" is the\nREADME's word, not the code's. The question is whether the README uses it as a\nname for something this project declares under its own word. If it does, the\nsense says exactly that -- `In this project, a <word> is the README's name for\n<the code's word> ...` -- and still names the place the code's word is written\ndown: pull `code.concordance` for the code's own word if you need its key or\nfile. If the prose word names nothing the code shows, `surveys.attest(\noutcome=\"none_found\")` -- for a prose word that is the common honest ending,\nnot the rare one.\n\n`code.source` a file the concordance makes you want to read whole \u2014 the one\nthat declares the word is usually enough.\n\n`[code.concordance]` also carries the file that declares the word, whole where\nit fits. Read it: what the word *does* is in that file, not in the three lines\nthat mention it.\n\nEnd with one `glossary.amend`, for this word only: `sense_short` the sentence\nin one clause; `sense_body` two to four sentences a developer who has never\nseen the code could act on -- where it is written down and by whom, what the\ncode does with it, what it relates to (the other words of this project it is\nmade of or makes), and what would break if it meant the everyday thing here.\nThat is the session's result, and the session ends with it.",
  "user": "You were woken by: tick:define\nRefs: @term:intent\nThe word: intent\n\n[code.concordance]\ndeclared as a kind:\n  src/variables/index.ts:7  export enum ReservedVariableName {\n      8: intent_name = \"intent_name\",\nin what a user writes:\n  intentsSchema.yaml:1  # Schema of intents frontmatter\n  intentsSchema.yaml:5  intents_imported_from: \"'text'|['text']\"\n  intentsSchema.yaml:6  intents_to:\n  intentsSchema.yaml:10  with_name: \"text ({{intent_name}})\"\n  intentsSchema.yaml:18  with_name: \"text ({{intent_name}})\"\na file of its own:\n  src/intents/intents.ts\n      7: type NoteDestination = {\n      12: class IntentSuggestModal extends FuzzySuggestModal<Intent> {\n      41: const shownIntents = intents.filter(i => !i.hidden);\n      60: const abstractIntentSource = plugin.app.vault.getAbstractFileByPath( intent.sourceNotePath );\n      69: const chosenTemplate = await getIntentTemplate(intent);\ndeclared:\n  src/intents/index.ts:7  export type Intent = hasNewNoteProperties & {\n  src/intents/frontmatter.ts:20  function getIntentsFromFM(app: App, fm: FrontMatterCache, sourceFile: TFile): Intent[] {\n  src/intents/frontmatter.ts:21  const newIntents: Intent[] = (fm?.intents_to || []).map((iFm: any): Intent => {\n  src/main.ts:38  const chosenIntent = await choseIntent( noteIntentsWithGlobalIntents );\n  src/main.ts:84  const chosenIntent = await choseIntent(this.settings.intents);\n  src/main.ts:112  const chosenIntent = await choseIntent( noteIntentsWithGlobalIntents );\nused:\n  src/main.ts  [.]  x61\n      29: const intentNote = this.app.workspace.getActiveFile()\n      36: const noteIntents = await getIntentsFromTFile(this.app, intentNote);\n  src/intents/intents.ts  [src/intents]  x42\n      77: const templatePath: string | void = resolvePathRelativeToAbstractFile(chosenTemplate.path, abstractIntentSourc\n  src/intents/frontmatter.ts  [src/intents]  x17\n      20: function getIntentsFromFM(app: App, fm: FrontMatterCache, sourceFile: TFile): Intent[] {\n      21: const newIntents: Intent[] = (fm?.intents_to || []).map((iFm: any): Intent => {\n  src/settings/settings.ts  [src]  x12\n      40: this.plugin.settings.globalIntentsNotePath = value + (!value.endsWith(\".md\") ? \".md\" : \"\");\n      8: globalIntentsNotePath: '',\n  src/templates/templates.ts  [src]  x8\n      11: const shownTemplates = intent.templates.filter(t => !t.hidden);\n      5: export async function getIntentTemplate(intent: Intent): Promise<Template | null> {\n  src/intents/index.ts  [src/intents]  x6\n      7: export type Intent = hasNewNoteProperties & {\nflows:\n  src/intents/frontmatter.ts -> intentsSchema.yaml\n  src/intents/frontmatter.ts -> src/intents/index.ts\n  src/intents/frontmatter.ts -> src/templates/index.ts\n  src/intents/frontmatter.ts -> src/variables/index.ts\n  src/intents/index.ts -> src/intents/frontmatter.ts\n  src/intents/index.ts -> src/intents/intents.ts\n  src/intents/index.ts -> src/templates/index.ts\n  src/intents/index.ts -> src/variables/index.ts\n\n----- src/intents/index.ts -----\nimport { TAbstractFile } from \"obsidian\";\nimport { Template } from \"../templates\";\nimport { TemplateVariable } from \"../variables\";\nimport { getIntentsFromTFile,  resolvePathRelativeToAbstractFile, namedObjectDeepMerge } from \"./frontmatter\";\nimport { choseIntent, getReplacedVariablesText, runIntent } from \"./intents\";\n\nexport type Intent = hasNewNoteProperties & {\n\tname: string;\n\thidden: boolean;\n\tdisabled: boolean;\n\ttemplates: Template[];\n\tsourceNotePath: string;\n}\n\nexport type hasNewNoteProperties = {\n\tnewNoteProperties: NewNoteProperties;\n}\n\nexport type NewNoteProperties = {\n\toutput_filename: string,\n\toutput_folder_path: string,\n\tvariables: TemplateVariable[],\n\tselection_replace_template?: string,\n}\n\nexport {\n\tgetIntentsFromTFile,\n\tresolvePathRelativeToAbstractFile,\n\tnamedObjectDeepMerge,\n\tchoseIntent,\n\trunIntent,\n\tgetReplacedVariablesText,\n};",
  "truth": {
   "must_not_contain": [
    "a specific action",
    "a task",
    "an intention",
    "a purpose",
    "a goal"
   ],
   "is_about": "a note-creation recipe declared in frontmatter"
  }
 },
 {
  "id": "define:provider",
  "kind": "define",
  "system": "You are terminologist.\n\nYou are Terminologist. You own the glossary: what each word this project uses\nmeans, here.\n\nYou are woken once, act, and end. You have no memory of previous sessions and\nwill have none of this one.\n\nMODE: define \u2014 one word, for the whole program.\n\n`[problem.baseline]` is what this program does for its user, as the Vision Keeper\nobserved it. `[code.concordance]` is the word you were woken for, everywhere\nthis project uses it, with what settles it first: what the code declares it to\nbe a kind of, what a user writes that says it, a file named for it, where it is\nregistered, then its declarations and its uses. The word is named on the\n`The word:` line. Weigh the top of the concordance over the bottom \u2014 a word the\ncode lists among kinds, or a user writes as an option, means that kind here\nwhatever the same word means in English.\n\nA word this project means nothing of its own by \u2014 ordinary English here, or a\nlibrary's word rather than this program's \u2014 is `surveys.attest(\noutcome=\"none_found\")`, and that is rare: the word was woken because the\nproject declares it. Otherwise, finish this sentence, in your own words, from\nthe concordance and the baseline:\n\n    In this project, a <word> is ...\n\nDefine the thing the code declares, not the program as a whole and not what the\nuser wants from it. Say what kind of thing it is, where a user or the code\nwrites it down \u2014 the exact key, type or file, spelled as the concordance spells\nit \u2014 and what it is for.\nThirty words at most. Test it by putting a different word in its place: if the\nsentence still reads true, it is a category and not a meaning.\n\nA concordance headed \"the code never says this word; the prose does\" is the\nREADME's word, not the code's. The question is whether the README uses it as a\nname for something this project declares under its own word. If it does, the\nsense says exactly that -- `In this project, a <word> is the README's name for\n<the code's word> ...` -- and still names the place the code's word is written\ndown: pull `code.concordance` for the code's own word if you need its key or\nfile. If the prose word names nothing the code shows, `surveys.attest(\noutcome=\"none_found\")` -- for a prose word that is the common honest ending,\nnot the rare one.\n\n`code.source` a file the concordance makes you want to read whole \u2014 the one\nthat declares the word is usually enough.\n\n`[code.concordance]` also carries the file that declares the word, whole where\nit fits. Read it: what the word *does* is in that file, not in the three lines\nthat mention it.\n\nEnd with one `glossary.amend`, for this word only: `sense_short` the sentence\nin one clause; `sense_body` two to four sentences a developer who has never\nseen the code could act on -- where it is written down and by whom, what the\ncode does with it, what it relates to (the other words of this project it is\nmade of or makes), and what would break if it meant the everyday thing here.\nThat is the session's result, and the session ends with it.",
  "user": "You were woken by: tick:define\nRefs: @term:provider\nThe word: provider\n\n[code.concordance]\ndeclared:\n  src/variables/providers/index.ts:54  export const variableProviderVariableParsers: {\n  src/variables/providers/index.ts:64  export const variableProviderVariableGetters: {\nused:\n  src/intents/frontmatter.ts  [src/intents]  x2\n      93: return Object.assign(baseVariables, variableProviderVariableParsers[type](app,v))\n      10: variableProviderVariableParsers,\n  src/variables/index.ts  [src/variables]  x2\n      46: variableProviderVariableParsers,\n  src/variables/providers/index.ts  [src/variables/providers]  x2\n      54: export const variableProviderVariableParsers: {\n      64: export const variableProviderVariableGetters: {\n  src/variables/templateVariables.ts  [src/variables]  x2\n      28: gatheredValues[variable.name] = await variableProviderVariableGetters[variable.type](app, variable, val, sourc\nflows:\n  src/intents/frontmatter.ts -> src/variables/index.ts\n  src/variables/index.ts -> src/variables/providers/index.ts\n  src/variables/index.ts -> src/variables/templateVariables.ts\n  src/variables/providers/index.ts -> src/variables/index.ts\n  src/variables/templateVariables.ts -> src/variables/index.ts\n  src/variables/templateVariables.ts -> src/variables/providers/index.ts\n\n----- src/variables/providers/index.ts (first part only) -----\nimport { App } from \"obsidian\";\nimport { TemplateVariable } from \"..\";\nimport { \n  TemplateVariableVariables_Text, \n  getTextVariableValue, \n  parseTextVariableFrontmatter \n} from \"./text\";\nimport { TemplateVariableVariables_Number, \n  getNumberVariableValue, \n  parseNumberVariableFrontmatter \n} from \"./number\";\nimport {\n  TemplateVariableVariables_NaturalDate,\n  getNaturalDateVariableValue, \n  parseNaturalDateVariableFrontmatter\n} from \"./natural_date\";\nimport {\n  TemplateVariableVariables_Folder,\n  getFolderVariableValue,\n  parseFolderVariableFrontmatter\n} from \"./folder\";\nimport { \n  TemplateVariableVariables_Note, \n  getNoteVariableValue, \n  parseNoteVariableFrontmatter \n} from \"./note\";\n\nexport enum TemplateVariableType {\n  text = \"text\",\n  number = \"number\",\n  natural_date = \"natural_date\",\n  note = \"note\",\n  folder = \"folder\",\n}\n\nexport type TemplateVariableVariables =\n  | TemplateVariableVariables_Text\n  | TemplateVariableVariables_Number\n  | TemplateVariableVariables_NaturalDate\n  | TemplateVariableVariables_Folder;\n\n\nexport type TemplateVariableVariablesLut = {\n  [T in TemplateVariableType]: {\n    [TemplateVariableType.text]: TemplateVariableVariables_Text,\n    [TemplateVariableType.number]: TemplateVariableVariables_Number,\n    [TemplateVariableType.natural_date]: TemplateVariableVariables_NaturalDate,\n    [TemplateVariableType.note]: TemplateVariableVariables_Note,\n    [TemplateVariableType.folder]: TemplateVariableVariables_Folder,\n  }[T]\n};\n\n\nexport const variableProviderVariableParsers: {\n  [K in keyof TemplateVariableVariablesLut]: (app: App, fm: any) => TemplateVariableVariablesLut[K];\n} = {\n  [TemplateVariableType.text]: parseTextVariableFrontmatter,\n  [TemplateVariableType.number]: parseNumberVariableFrontmatter,\n  [TemplateVariableType.natural_date]: parseNaturalDateVariableFrontmatter,\n  [TemplateVariableType.note]: parseNoteVariableFrontmatter,\n  [TemplateVariableType.folder]: parseFolderVariableFrontmatter,\n};\n\nexport const variableProviderVar",
  "truth": {
   "must_not_contain": [
    "a service",
    "a dependency injection",
    "a data source",
    "an API client"
   ],
   "is_about": "the module serving one prompt type"
  }
 }
]