# A development checkout of rota. `pip install -r requirements.txt`.
#
# `pyproject.toml` states what the package needs, and is the file to change
# when a dependency arrives or leaves. This file pins the versions the suite
# was last measured against. It also adds the test tools, which the package
# does not need in order to run.

# Runtime.
textual==8.2.5
rich==15.0.0
PyYAML==6.0.3
tree-sitter==0.25.2
tree-sitter-language-pack==1.14.3
watchfiles==1.1.1

# Optional, and not pinned. `rota.llm.discover` reports host memory when
# psutil is present, and the litellm profile needs litellm. The suite runs
# without either.
#   psutil
#   litellm==1.96.0

# Tests.
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-timeout==2.4.0
pytest-xdist==3.8.0
