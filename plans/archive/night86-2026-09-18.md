# Night 86, the first batch and the wall behind it

Rota workflow. Night 86 ran cold on click from a worktree at 8b15af8,
from sentence two, on 2026-09-18 from 06:08. The run database is
`.rota/clickI_night86.db`. The log is `.rota/night86.log`.

## The result

**A cold night reached a batch and a commit for the first time in this
series.** Onboarding ran 06:08 to 06:49. Sentence two ran 77 steps with
7 asks, where night 85 ran 10 steps with 2 asks and went quiet.

The delivery path now runs end to end up to the tests (observed: the run
database):

- No present stayed open. Night 85's `m27` was open all night.
- Five items reached approval `approved`. Night 85's stayed `draft`.
- One ticket, and one batch `bg_1` for the item `default_on_eof`, which
  is sentence two's own request.
- The batch took a worktree and committed at `da5e7e8`.

Four walls came down to get here, each fixed in this session and each
needed. The parser door at 65ca902 let the observed-entries page
through. The ruling fix at a268266 let a contested answer park the word
instead of looping. The page fix at 8ebbae1 let the seat see its brief.
The gate fix at 8b15af8 stopped the duplicate rows from slipping past
the size check.

## The wall

The night went quiet after the commit. The Developer could not make the
tests pass.

Its wakes, in order (observed: the `sessions` table):

    s145  tick:batch_start
    s146  tick:tests_failing   attempt 1
    s147  tick:tests_failing   attempt 1
    s148  tick:tests_failing   attempt 1
    s154  tick:tests_failing   attempt 3
    s156  tick:tests_failing   attempt 3
    s158  tick:tests_failing   attempt 4
    s160  tick:tests_failing   attempt 5
    s162  tick:tests_failing   attempt 6
    s164  tick:tests_failing   attempt 7
    s166  tick:tests_failing   attempt 8
    s168  tick:tests_failing   attempt 9
    s170  tick:exhausted       attempt 10 of 10

Every session committed. The batch stayed `running`. There are 37 test
runs and **no verdict of any result**.

The Critic woke 22 times, on `tick:challenge` only, and never on a
review tick (observed: the `sessions` table grouped by wake). That
confirms the parked observation of frame 2, made on nights 81 and 82.
The Critic reviews a batch that passes its tests, and no batch has ever
passed.

## What this changes

The wall is no longer in the records path. It is in the work itself: a
9B Developer cannot make this change pass its tests in ten attempts.
That is the question the composition calls being honest about where
small models stop.

Night 82 was stuck on `exhausted` at sentence three, 80 steps (frame 13,
`plans/archive/stack-2026-09-16.md`). Night 86 is stuck on `exhausted`
at sentence two. The same wall now stands one sentence earlier, because
sentence two's earlier walls are gone.

Sentence two merged cold on night 70 at an older commit. That merge is
the evidence that this is not a permanent ceiling. What changed between
night 70 and night 86 is not recorded here and is the first thing to
read.

## What is still owed

Frame 33's evidence is not in. `config.project_commit` still names the
base commit `2c8cd3a`, so the main index never refreshed, because the
refresh hook keys on the merge and no merge happened. The index refresh
of frame 33 is built, reviewed, gated and unwalked.

## Open messages at the quiet point

    m41  report   terminologist -> liaison
    m42  report   architect     -> liaison
    m81  deliver  liaison -> vision_keeper
    m82  deliver  liaison -> terminologist
    m83  deliver  liaison -> architect

Five open messages at a quiet run. Whether a quiet run with open
messages is correct is a separate question from the Developer's wall,
and it is not answered here.
